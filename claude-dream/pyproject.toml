[project]
name = "claude-dream"
version = "0.3.0"
description = "Personal knowledge base plugin for Claude Code - compiled from AI conversations"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv>=1.0.0",
    "tzdata>=2024.1",
]

[tool.ruff]
line-length = 100
